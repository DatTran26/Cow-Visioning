Place your YOLO `.pt` or `.onnx` model file in this directory.

Default expected path:

- `models/best.pt`
- `models/best.onnx`

Or override it with:

- `AI_MODEL_PATH=/absolute/or/relative/path/to/your-model.pt`
- `AI_MODEL_PATH=/absolute/or/relative/path/to/your-model.onnx`
